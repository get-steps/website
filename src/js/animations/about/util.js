// export const DEFAULT_DURATION = 1500;

// export const selectSectionComponents = $elem => {
//  e.g. const $header = $elem.find('.s-about-info-header');

//   return [$header, $text, $cta, $images];
// };
